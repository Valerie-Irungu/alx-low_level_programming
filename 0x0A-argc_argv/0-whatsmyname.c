#include <stdio.h>

/**
 * main - prints the program name
 * @argc: argurments in the program
 * @argv: an array of pointers to the argurment
 * Return: 0 on Success
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
