#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a: An integer that will be multiplied with b
 * @b: An integer that will be multiplied with a
 * Return: The result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
